"""Command-line entry points.

The ms-denovo-db Nextflow pipeline invokes these through thin shims installed
at ``/usr/local/bin/<name>.py``; the console scripts declared in
pyproject.toml expose the same functions under plain command names.
"""
